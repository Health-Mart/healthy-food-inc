/* eslint-disable prefer-const */
/* eslint-disable no-unused-vars */
// recipeMeta Data

// array
const recipeMeta = [ '123456' ];

// add this recipe of
ADD id: 23456

let data = {
  title: '',
  isSelect: false
};

recipeMeta.map((item) => {
  let result = false;
  if (item.id !== id) {
    result = true;
  }
  if (result) {
    recipeMeta.push(data);
  }
});
